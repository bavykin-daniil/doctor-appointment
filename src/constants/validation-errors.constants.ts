export enum ERRORS {
  EMAIL = "Invalid email format",
  REQUIRED = "Field is required!",
  MIN_DATA = "Date should be not more than a 100 years ago",
  MAX_DATA = "Date shouldn't be in the future",
  NUMBER_FORMAT = "Enter correct number in +380 format",
  NOT_CONTAIN_NUMBERS = "Field should not contain numbers",
}
